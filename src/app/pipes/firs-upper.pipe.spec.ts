import { FirsUpperPipe } from './firs-upper.pipe';

describe('FirsUpperPipe', () => {
  it('create an instance', () => {
    const pipe = new FirsUpperPipe();
    expect(pipe).toBeTruthy();
  });
});
